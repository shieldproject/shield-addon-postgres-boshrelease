#!/bin/sh
set -eux
CPUS=$(grep -c ^processor /proc/cpuinfo)

tar -xjf postgres/postgresql-17.9.tar.bz2
cd postgresql-17.9
./configure --prefix=${BOSH_INSTALL_TARGET} --with-openssl --without-icu
make -j$CPUS && make install
